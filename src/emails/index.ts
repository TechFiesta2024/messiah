export const fpga_email = await Bun.file("./src/emails/fpga_email.html").text();
export const backend_deploy_email = await Bun.file(
	"./src/emails/backend_deploy_email.html",
).text();
export const circuit_email = await Bun.file(
	"./src/emails/circuit_email.html",
).text();
export const iot_workshop_email = await Bun.file(
	"./src/emails/iot_workshop_email.html",
).text();
export const business_logic_email = await Bun.file(
	"./src/emails/business_logic_email.html",
).text();
export const ctf_signup_email = await Bun.file(
	"./src/emails/ctf_signup_email.html",
).text();
export const git_github_email = await Bun.file(
	"./src/emails/git_github_email.html",
).text();
export const product_design_cycle_email = await Bun.file(
	"./src/emails/product_design_cycle_email.html",
).text();
export const welcome_email = await Bun.file(
	"./src/emails/welcome_email.html",
).text();
export const cad_signup_email = await Bun.file(
	"./src/emails/cad_signup_email.html",
).text();
export const embedded_systems = await Bun.file(
	"./src/emails/embedded_systems.html",
).text();
export const robotics_email = await Bun.file(
	"./src/emails/robotics_email.html",
).text();
